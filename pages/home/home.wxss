.page {
  padding: 20rpx 24rpx 160rpx;
}

.search-bar {
  margin-bottom: 16rpx;
}

.search-input {
  background: #fff;
  border-radius: 999rpx;
  padding: 0 32rpx;
  height: 72rpx;
  line-height: 72rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

/* 空间切换 */
.space-scroll {
  white-space: nowrap;
  margin-bottom: 16rpx;
}

.space-row {
  display: inline-flex;
  align-items: center;
}

.space-chip {
  display: inline-flex;
  align-items: center;
  background: #fff;
  color: #666;
  font-size: 26rpx;
  padding: 12rpx 28rpx;
  border-radius: 999rpx;
  margin-right: 14rpx;
  font-weight: 500;
}

.space-active {
  background: var(--primary, #FF7A45);
  color: #fff;
  font-weight: 600;
}

.space-add {
  background: #FFF3EE;
  color: var(--primary, #FF7A45);
}

/* 搜索历史 */
.history {
  background: #fff;
  border-radius: 16rpx;
  padding: 16rpx 20rpx;
  margin-bottom: 16rpx;
}

.history-head {
  display: flex;
  justify-content: space-between;
  margin-bottom: 12rpx;
}

.history-title {
  font-size: 24rpx;
  color: #8A8A90;
}

.history-clear {
  font-size: 24rpx;
  color: var(--primary, #FF7A45);
}

.history-chips {
  display: flex;
  flex-wrap: wrap;
}

.history-chip {
  background: #F5F5F7;
  color: #666;
  font-size: 24rpx;
  padding: 8rpx 20rpx;
  border-radius: 999rpx;
  margin: 0 12rpx 12rpx 0;
}

/* 新手引导遮罩 */
.mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.55);
  z-index: 999;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 60rpx;
}

.guide {
  background: #fff;
  border-radius: 32rpx;
  padding: 48rpx 40rpx;
  width: 100%;
  text-align: center;
}

.guide-logo {
  font-size: 88rpx;
}

.guide-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #1A1A1A;
  margin-top: 16rpx;
}

.guide-sub {
  font-size: 26rpx;
  color: #8A8A90;
  margin-top: 12rpx;
  line-height: 1.5;
}

.guide-steps {
  margin: 36rpx 0;
  text-align: left;
}

.guide-step {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
}

.gs-icon {
  font-size: 40rpx;
  margin-right: 20rpx;
}

.gs-text {
  font-size: 28rpx;
  color: #333;
}

.guide-btn {
  background: var(--primary, #FF7A45);
  color: #fff;
  border-radius: 999rpx;
  padding: 24rpx;
  font-size: 32rpx;
  font-weight: 600;
}

/* 数据仪表盘 */
.dash {
  background: #fff;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.dash-top {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 20rpx;
}

.dash-sub {
  font-size: 30rpx;
  font-weight: 600;
  color: #1A1A1A;
}

.dash-value {
  font-size: 24rpx;
  color: var(--primary, #FF7A45);
  font-weight: 600;
}

.dash-stats {
  display: flex;
  border-top: 1rpx solid #F2F2F4;
  padding-top: 20rpx;
  margin-bottom: 16rpx;
}

.dstat {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.dstat-num {
  font-size: 40rpx;
  font-weight: 700;
  color: #1A1A1A;
}

.dstat-label {
  font-size: 22rpx;
  color: #8A8A90;
  margin-top: 4rpx;
}

.d-warn {
  color: #FFA940;
}

.d-danger {
  color: #FF4D4F;
}

.dash-bars {
  border-top: 1rpx solid #F2F2F4;
  padding-top: 16rpx;
}

.dbar {
  display: flex;
  align-items: center;
  padding: 8rpx 0;
}

.dbar-icon {
  width: 40rpx;
  font-size: 24rpx;
}

.dbar-name {
  width: 110rpx;
  font-size: 24rpx;
  color: #666;
  flex-shrink: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.dbar-track {
  flex: 1;
  height: 14rpx;
  border-radius: 999rpx;
  background: #F0F0F2;
  margin: 0 16rpx;
  overflow: hidden;
}

.dbar-fill {
  height: 100%;
  border-radius: 999rpx;
}

.dbar-pct {
  width: 64rpx;
  font-size: 22rpx;
  color: #8A8A90;
  text-align: right;
}

/* 视图切换 */
.seg {
  display: flex;
  background: #E9E9EC;
  border-radius: 999rpx;
  padding: 4rpx;
  margin-bottom: 16rpx;
}

.seg-item {
  flex: 1;
  text-align: center;
  padding: 12rpx 0;
  font-size: 26rpx;
  color: #666;
  border-radius: 999rpx;
}

.seg-active {
  background: #fff;
  color: #1A1A1A;
  font-weight: 600;
  box-shadow: 0 2rpx 6rpx rgba(0, 0, 0, 0.06);
}

.reminder {
  background: linear-gradient(135deg, #FFB199, #FF8A65);
  color: #fff;
  border-radius: 20rpx;
  padding: 20rpx 24rpx;
  margin-bottom: 16rpx;
  display: flex;
  flex-direction: column;
  font-size: 28rpx;
  position: relative;
}

.rem-line {
  line-height: 1.5;
}

.arrow {
  position: absolute;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 40rpx;
}

.filter-scroll {
  white-space: nowrap;
  margin-bottom: 12rpx;
}

.filter-row {
  display: inline-flex;
}

.chip {
  display: inline-block;
  background: #fff;
  color: #666;
  font-size: 26rpx;
  padding: 10rpx 24rpx;
  border-radius: 999rpx;
  margin-right: 14rpx;
}

.chip-active {
  background: var(--primary, #FF7A45);
  color: #fff;
}

.room-group {
  margin-bottom: 8rpx;
}

.room-head {
  font-size: 26rpx;
  font-weight: 600;
  color: #1A1A1A;
  margin: 12rpx 4rpx 16rpx;
}

.empty-hero {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 80rpx 40rpx;
}

.eh-icon {
  font-size: 96rpx;
  margin-bottom: 24rpx;
}

.eh-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #1A1A1A;
  margin-bottom: 12rpx;
}

.eh-sub {
  font-size: 26rpx;
  color: #8A8A90;
  text-align: center;
  line-height: 1.6;
  margin-bottom: 40rpx;
  white-space: pre-line;
}

.eh-btn {
  width: 100%;
  text-align: center;
  padding: 24rpx;
  border-radius: 999rpx;
  font-size: 30rpx;
  margin-bottom: 20rpx;
}

.eh-primary {
  background: var(--primary, #FF7A45);
  color: #fff;
  font-weight: 600;
}

.eh-ghost {
  background: #fff;
  color: var(--primary, #FF7A45);
}

.fab {
  position: fixed;
  right: 40rpx;
  bottom: 60rpx;
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  background: var(--primary, #FF7A45);
  color: #fff;
  font-size: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(255, 122, 69, 0.4);
}
