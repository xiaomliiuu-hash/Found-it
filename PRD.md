# 《小寻物仓》产品需求文档（PRD）

| 项 | 内容 |
|---|---|
| 文档名称 | 小寻物仓 产品需求文档 |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-18 |
| 适用对象 | 开发 / 产品 / 测试 |
| 关联工程 AppID | `wx861df87d70fa50ea` |
| 技术形态 | 原生微信小程序（纯本地存储，无后端） |

---

## 1. 产品概述

### 1.1 一句话定位

**记住每件东西放在哪、什么时候过期、什么时候该补货。**

### 1.2 产品名称

- 正式名：**小寻物仓**
- 曾用名：找到啦（已全量改名，代码中不再出现）

### 1.3 核心价值

| 价值点 | 说明 |
|---|---|
| 位置记忆 | 记录物品放在哪个空间、哪个房间、哪个具体位置，找东西不再翻箱倒柜 |
| 保质期提醒 | 记录到期日与开封后保质期，临期/过期自动高亮提醒 |
| 低库存补货 | 按「低库存阈值」自动生成购物清单，用完/缺货一眼可见 |
| 多空间管理 | 家 / 办公室 / 车 / 老家等不同空间各自隔离管理 |

### 1.4 产品定位

面向**个人与家庭**的轻量物品收纳工具，开箱即用、无需注册登录、数据完全本地。

---

## 2. 产品范围与技术架构

### 2.1 技术形态

- **原生微信小程序**：WXML + WXSS + JS + JSON，不使用任何框架与第三方 UI 库。
- 基础库版本 `libVersion: 2.33.0`；`lazyCodeLoading: requiredComponents`。

### 2.2 数据存储（纯本地，无后端）

- **结构化数据**：通过 `wx.setStorageSync` / `wx.getStorageSync` 存储，全部使用 `fdl_` 前缀的 key。
- **照片文件**：通过 `FileSystemManager.saveFile` 持久化到本地用户目录（临时路径会被系统清理，必须 saveFile）。
- **无后端 / 无账号 / 无云 / 无网络请求**：`urlCheck: false`，小程序不发起任何业务网络请求。

### 2.3 目录结构

```
├── app.js / app.json / app.wxss        全局入口、配置、样式
├── project.config.json                 工程配置（appid / libVersion 等）
├── sitemap.json
├── components/
│   ├── item-card/                      物品卡片组件
│   └── empty-state/                    空状态组件
├── pages/
│   ├── home/                           首页（物品）
│   ├── expiry/                         过期提醒
│   ├── shopping/                       购物清单
│   ├── settings/                       我的
│   ├── item-detail/                    物品详情
│   ├── item-edit/                      添加 / 编辑物品
│   ├── category-edit/                  新建 / 编辑分类
│   └── trash/                          回收站
└── utils/
    ├── store.js                        本地存储封装（数据层）
    ├── date.js                         日期与过期计算（纯函数）
    ├── stats.js                        汇总统计（纯函数）
    ├── templates.js                    常用模板 + 示例数据
    ├── csv.js                          CSV 导出
    ├── theme.js                        主题色
    ├── id.js                           ID 生成
    └── file.js                         照片持久化
```

共 8 个页面、2 个自定义组件、8 个工具模块。

---

## 3. 目标用户与使用场景

### 3.1 目标用户

- 家庭整理者：管理家中药品、食品、日用品、工具、证件。
- 囤货党 / 精致生活人群：关心有效期、开封后保质期、补货提醒。
- 多场景人群：家、办公室、车、老家等多地点分别存放物品。

### 3.2 典型场景

| 场景 | 说明 |
|---|---|
| 找东西 | 输入关键词，按名称/位置/标签/分类快速定位「东西在哪」 |
| 药品/食品过期 | 记录到期日与开封日期，临期/过期高亮提醒，过期后一键归档 |
| 补货 | 低于阈值自动进入购物清单，用完显示「缺货」 |
| 借出管理 | 工具借给谁了、是否归还，一屏可见 |
| 重要证件 | 护照、证件放在哪个抽屉，避免临时翻找 |

---

## 4. 全局设计与导航

### 4.1 TabBar（4 个）

| Tab | 页面 | 说明 |
|---|---|---|
| 物品 | `pages/home/home` | 首页，物品列表 |
| 过期 | `pages/expiry/expiry` | 过期提醒 |
| 清单 | `pages/shopping/shopping` | 购物清单 |
| 我的 | `pages/settings/settings` | 设置与管理 |

Tab 选中色 `#FF7A45`（暖橙），未选中 `#9A9AA0`，仅文字无图标。

### 4.2 页面清单（8 个）与路由

| 页面 | 标题（navigationBarTitleText） | 说明 |
|---|---|---|
| home | 小寻物仓 | 物品首页 |
| expiry | 过期提醒 | 过期/临期列表 |
| shopping | 购物清单 | 低库存清单 |
| settings | 我的 | 设置 |
| item-detail | 物品详情 | 详情 + 四大操作 |
| item-edit | 添加物品（编辑时动态改为「编辑物品」） | 表单 |
| category-edit | 新建分类（编辑时动态改为「编辑分类」） | 分类表单 |
| trash | 回收站 | 软删除管理 |

导航关系：

```
Tab: 物品 → 过期 → 清单 → 我的
home      → item-edit（FAB/空状态）；item-detail（卡片点击）；expiry（仪表盘/横幅 switchTab）
item-detail → item-edit（编辑）
expiry / shopping → item-detail
settings  → category-edit（新增/编辑）；trash（回收站）
```

### 4.3 主题系统

| 主题 | 颜色 |
|---|---|
| 暖橙（默认） | `#FF7A45` |
| 清新蓝 | `#5B8DEF` |
| 抹茶绿 | `#3FB68B` |
| 樱花紫 | `#9B59B6` |

- 主题色通过 `wx.setNavigationBarColor` 应用到导航栏（前景白字），并作为 CSS 变量 `--primary` 注入页面。
- 全局样式在 `app.wxss`：`page { --primary: #FF7A45; background: #F5F6F8; color: #1A1A1A; font-size: 28rpx; }`。

### 4.4 新手引导

- 首次启动（`settings.onboarded !== true`）在首页展示全屏引导遮罩：Logo、三步说明（📦 记录物品 / ⏰ 到期提醒 / 🛒 补货清单）、「开始记录 →」按钮。
- 点击按钮或遮罩外部即关闭并置 `onboarded = true`。

---

## 5. 核心数据模型

### 5.1 存储 Key 对照表

| Key | 类型 | 含义 |
|---|---|---|
| `fdl_items` | `Item[]` | 物品数组 |
| `fdl_members` | `Member[]` | 成员 |
| `fdl_categories` | `Category[]` | 分类 |
| `fdl_rooms` | `{ [spaceId]: string[] }` | 房间（按空间分桶；旧版为全局数组） |
| `fdl_spaces` | `Space[]` | 空间 |
| `fdl_trash` | `TrashEntry[]` | 回收站 |
| `fdl_search_history` | `string[]` | 搜索历史（最多 10 条） |
| `fdl_settings` | `Settings` | 设置 |

### 5.2 实体 Schema

**Item（物品）**

```js
{
  id: string,                // genId('it')，如 it_xxxxxx_abcdef
  name: string,              // 名称（必填）
  photos: string[],          // 本地持久化照片路径，最多 9 张
  location: { room: string, place: string },  // 房间 / 具体位置
  categoryId: string,        // 分类 id，'' 表示未分类
  spaceId: string,           // 所属空间 id
  tags: string[],            // 标签
  memberId: string,          // 归属人 id
  expiry: {                  // 保质期提醒
    enabled: boolean,
    date: string,            // 'YYYY-MM-DD' | ''
    alertDays: number        // 提前提醒天数（默认取设置的 defaultAlertDays）
  },
  quantity: number | null,   // 数量，null 表示不记数量
  unit: string,              // 单位，如 盒/瓶/件
  opened: {                  // 开封后保质期
    openedAt: string,        // 'YYYY-MM-DD' | ''
    shelfDays: number        // 开封后保质天数
  },
  purchase: {                // 购买信息
    date: string,            // 购买日期
    store: string,           // 购买渠道
    price: string            // 价格（字符串存储，统计时解析）
  },
  lowStockThreshold: number | null,  // 低库存阈值
  borrowed: { enabled: boolean, to: string },  // 借出中 / 借给谁
  note: string,              // 备注
  createdAt: number,         // 创建时间戳（ms）
  updatedAt: number,         // 更新时间戳（ms）
  archived: boolean,         // 归档标志（v4）
  demo: boolean              // 示例数据标志（v4）
}
```

**Space（空间）**

```js
{ id: string, name: string, icon: string }  // id genId('sp')；icon 为 emoji
```

**Member（成员）**

```js
{ id: string, name: string, color: string } // id genId('m')；color 为十六进制色
```

**Category（分类）**

```js
{ id: string, name: string, icon: string, color: string } // id genId('c')
```

**Room（房间）**：纯字符串（如 `'客厅'`），按空间分桶存储 `rooms[spaceId] = string[]`。

**TrashEntry（回收站条目）**

```js
{ item: Item, deletedAt: number }  // deletedAt 为删除时间戳
```

**搜索历史**：`string[]`，最近优先，去重，最多 10 条。

**Settings（设置）**

```js
{
  defaultAlertDays: 3,      // 默认提前提醒天数
  notifyEnabled: true,      // 到期提醒开关
  themeColor: '#FF7A45',    // 主题色
  onboarded: false,         // 是否已看过新手引导
  currentSpaceId: 'sp_home',// 当前空间
  seedVersion: 4            // 数据迁移版本
}
```

### 5.3 ID 生成

`genId(prefix)` → `prefix + '_' + Date.now().toString(36) + '_' + 随机6位`。

| 前缀 | 实体 |
|---|---|
| `it` | 物品 |
| `sp` | 空间 |
| `m` | 成员 |
| `c` | 分类 |

### 5.4 默认种子数据

- **默认分类（14 个）**：食品🍎 / 药品💊 / 美妆💄 / 衣物👕 / 工具🔧 / 电子📱 / 清洁🧹 / 母婴🍼 / 宠物🐾 / 家电🔌 / 运动⚽ / 图书📚 / 文件📄 / 其他📦。
- **默认成员（1 个）**：`{ id: 'm_me', name: '我', color: '#FF7A45' }`。
- **默认空间（1 个）**：`{ id: 'sp_home', name: '我的家', icon: '🏠' }`。
- **默认房间（9 个，归属默认空间）**：客厅、卧室、厨房、卫生间、阳台、书房、储物间、车库、其他。

---

## 6. 功能需求详述

### 6.1 首页（物品）

**功能点**

1. **空间切换与新增**
   - 顶部横向滚动空间胶囊（`图标 + 名称`）+ `＋ 新空间` 胶囊。
   - 点击空间切换为当前空间（持久化到 `settings.currentSpaceId`）。
   - 新增空间：弹窗输入空间名（placeholder「空间名，如：办公室」），图标按 `SPACE_ICONS`（🏠🏢🚗🏡🏬🎒）轮转自动分配，新增后自动切换为当前空间。

2. **搜索**
   - 搜索框 placeholder「搜索名称/位置/标签/分类」。
   - 实时过滤 + 确认时写入搜索历史。
   - **匹配规则**：多关键词按空格拆分后 **AND** 匹配；每个关键词支持**子串或子序列**匹配；搜索字段包括名称、房间、位置、标签、分类名、成员名、单位、备注、购买渠道、借给谁、到期日、开封日期。
   - **搜索历史**：输入框聚焦且为空、且历史非空时展示；「最近搜索」+「清除」；点历史词直接搜索。最多 10 条，最近优先。

3. **数据仪表盘**
   - 显示 `{空间名} · 共 N 件`、累计价值 `¥…`（有购买价格时）。
   - 三个统计：总物品 / 临期 / 已过期；分类分布条（可点击作为分类筛选切换）。

4. **视图切换**：`列表` / `按房间` 分段切换。
   - 列表视图：按 `createdAt` 倒序（最新在前）。
   - 按房间视图：按 `location.room` 分组，未设置位置的归入「未设置位置」放最后，其余按物品数量降序。

5. **到期提醒横幅**：`⏰ 「名称」X 天后过期`（或「今天到期」），当 `notifyEnabled` 且有最早到期物品时展示；点击跳转「过期」页。

6. **快捷筛选**：`全部 / 快过期 / 低库存 / 借出中 / 已归档`（单选切换）。
   - `已归档` 切换查看已归档物品。

7. **分类筛选 / 成员筛选**：分类胶囊（`全部分类` + 各分类）；成员胶囊仅在成员数 > 1 时展示。

8. **示例数据**：空状态时提供 `🎁 一键体验示例数据`，生成 7 条示例物品（`demo: true`，覆盖过期/临期/低库存/借出/用完等状态），仅当当前空间无物品时生成。

9. **FAB（+）**：悬浮按钮，进入添加物品页。

10. **空状态**：无物品时显示引导空状态（`＋ 添加第一件物品` + `🎁 一键体验示例数据`）；有物品但筛选无结果显示「没有找到相关物品」。

### 6.2 过期提醒

**功能点**

- 两个列表：
  - **已过期（N）**：按剩余天数升序（过期最久的在前）。
  - **即将过期（N）**：按剩余天数升序（最先到期的在前）。
- 数据范围：当前空间 + 未归档。
- **一键归档**：仅当存在已过期物品时显示；确认弹窗「将 N 件已过期物品归档，归档后不再首页提醒（可在首页「已归档」查看）。」→ 归档全部已过期物品 → toast「已归档 N 件」。
- 点击卡片 → 物品详情。
- 空状态：`太棒了，没有需要处理的过期物品`。

### 6.3 购物清单

**功能点**

- 自动生成低库存清单：筛选当前空间、未归档、`quantity <= lowStockThreshold` 的物品，按缺额升序（缺得最多的在前）。
- 每张卡片：勾选框 + 分类图标 + 名称 + `当前 X单位 · 阈值 Y单位` + 徽章。
  - `缺货`（红）：`quantity === 0`。
  - `补 N单位`（橙）：`quantity > 0`（N = 阈值 − 数量）。
- 勾选为本地视觉切换（不持久化）。
- 点击卡片 → 物品详情。
- 空状态：`没有需要补货的物品`。

### 6.4 我的（设置）

**功能点**

1. **统计行**：物品数（未归档）/ 成员数 / 分类数。
2. **空间管理**：列表展示（当前空间带「当前」徽章，可点击切换）；`＋ 添加空间`；删除（至少保留一个；若空间下有物品，删除时物品移入回收站）。
3. **成员管理**：颜色圆点 + 删除；`＋ 添加成员`（颜色自动轮转）；至少保留一个成员。
4. **分类管理**：列表（点击进入编辑）；`＋ 添加分类`。
5. **位置管理（房间）**：当前空间的房间胶囊，每个带 `×` 删除（删除不影响已记录该房间的物品）；`＋ 添加房间`。
6. **消息通知**：`到期提醒（打开 App 时）` 开关；`默认提前提醒` 天数选择（`1/2/3/5/7/14/30` 天）。
7. **主题**：4 个主题色切换。
8. **数据**：
   - `导出 Excel（CSV）`：动作面板选择「发送到微信保存」/「复制到剪贴板」。
   - `导出备份（JSON）`：复制到剪贴板。
   - `导入备份（从剪贴板）`：解析剪贴板 JSON，确认后覆盖导入（照片不导入）。
   - `回收站`：入口（带数量徽章）。
   - `清空示例数据`：仅当存在示例数据时显示（带数量徽章）。
   - `重新查看新手引导`。
   - `存储占用`：只读展示当前存储占用。
9. **关于**：`小寻物仓 v4.0 · 记住每件东西在哪`。

### 6.5 物品详情

**功能点**

- **照片**：有照片则轮播（点击 `wx.previewImage` 预览）；无照片则展示大分类图标占位。
- **名称行**：名称 + 归档提示 + 过期徽章（如 `2026-01-01 · 剩 3 天`）。
- **信息卡片**：数量、位置（📍，未设置显示「未设置」）、分类、归属、标签、过期（开封后保质期时提示「按开封后保质期」）、开封、购买、库存提醒（`低于 N 件提醒`）、借出（`借给 X` / `已借出`）、备注。

**四大操作（底部固定栏）**

| 操作 | 图标 | 行为 |
|---|---|---|
| 移动位置 | 📍 | 底部弹层：房间胶囊 + 「具体位置」输入 → 更新 location |
| 借出 / 归还 | 🤝 / ↩️ | 切换；借出时弹窗输入「借给谁」；归还时清空并 toast「已归还」 |
| 用完 | ✅ | 数量置 0；已用完则提示「已经是用完状态」 |
| 丢弃 | 🗑️ | 二次确认后软删除移入回收站 |

**其他操作**：`编辑`（进入编辑页）、`归档` / `取消归档`（切换 `archived`，toast「已归档，不再提醒」/「已取消归档」）。

### 6.6 物品添加 / 编辑

**功能点**

1. **常用模板（仅新增模式）**：14 条模板（牛奶/鸡蛋/蔬菜/大米/食用油/感冒药/退烧药/洗发水/口红/面霜/洗衣液/纸巾/电池/宠物粮）。点击**仅填充空字段**（名称/分类/单位/开封后保质天数），toast「已套用「X」，可继续补充」。
2. **照片与备注**：照片网格（最多 9 张，`wx.chooseMedia` 选择后 `savePhoto` 持久化；点击预览；`×` 删除并 `removePhoto`）；备注 textarea。
3. **基本信息**：名称（必填）、数量、单位（输入 + 单位胶囊 件/盒/瓶/包/个/袋/斤/支/罐）、房间（输入 + 当前空间房间胶囊）、具体位置。
4. **所属空间**：picker 选择空间；切换空间时重载该空间房间并清空房间。
5. **分类（分类漏斗）**：当前分类展示 + `🔽 筛选` 按钮，打开底部浮层分类网格（图标 + 名称）选择分类。
6. **归属人**：成员胶囊单选。
7. **标签**：标签胶囊（`×` 删除）+ 输入 + `添加` 按钮。
8. **保质期**：`保质期提醒` 开关；开启后显示 `到期日期`（日期选择）+ `提前提醒`（`1/2/3/5/7/14/30` 天）；另有 `开封日期` + `开封后保质`（天数）。实时显示剩余天数（见第 7 章）。
9. **购买信息（选填）**：购买日期、购买渠道、价格（digit 输入）。
10. **库存与借出**：低库存阈值（number）、`借出中` 开关 + `借给谁` 输入。
11. **底部栏**：`保存并继续添加`（仅新增模式；保留分类/成员/房间/空间/提醒天数/单位）、`保存`。

**校验**：名称为空时提示「请填写物品名称」。

### 6.7 分类新建 / 编辑

**功能点**

- 名称输入 + 实时预览（图标 + 名称，用分类色）。
- 图标选择：40 个 emoji。
- 颜色选择：14 种颜色。
- 底部：`删除`（仅编辑模式）+ `保存`。

**删除二次确认（v4）**

- 分类下 **0 件物品**：单次确认弹窗「确定删除「name」吗？」。
- 分类下 **N 件物品**：先确认「该分类下有 N 件物品，删除后如何处理？」，再动作面板二选一：
  - `移动到其他分类` → 再选择目标分类 → 批量改分类后删除。
  - `变为未分类` → 物品 `categoryId` 置空后删除。

### 6.8 回收站

**功能点**

- 标题 `回收站 · N 件`；卡片显示缩略图（或分类图标）、名称、`📍 房间 · 删除于 YYYY-MM-DD HH:mm`。
- **恢复**：物品移回物品列表（`restoreFromTrash`）。
- **彻底删除**：二次确认（`删除后无法恢复，照片也会一并删除。`）→ 删除条目 + 删除全部照片文件。
- **清空回收站**：二次确认（`将彻底删除回收站内全部物品及照片，无法恢复。`）→ 清空 + 删除全部照片。
- 空状态：`回收站为空`。

### 6.9 自定义组件

**item-card（物品卡片）**

- 属性：`item`、`category`、`member`。
- 根据物品计算并展示：
  - 过期徽章（`expired` 红 `#FF4D4F` / `soon` 橙 `#FFA940`，文案 `已过期 N 天` / `剩 N 天` / `今天到期`）。
  - 数量文本 `×数量单位`。
  - 标签：借出中（蓝）、用完（灰）、低库存（橙）。
  - **进度条**：开封后按 `已用/开封后保质` 比例；基础到期仅在提醒窗口内展示（`1 - remaining/alertDays`），已过期则满格。颜色 `p-green / p-orange / p-red`。
- **用完弱化**：`quantity === 0` 时整卡 `opacity 0.55`、缩略图灰度 + 透明度 0.7、显示灰色「用完」标签。
- 点击触发 `tap` 事件（携带 `{ id }`）。

**empty-state（空状态）**

- 属性：`icon`（默认 📦）、`text`（默认「暂无数据」）。

---

## 7. 关键规则与算法

### 7.1 到期计算（`getEffectiveExpiry`）

有效到期日 = **基础到期日** 与 **开封日期 + 开封后保质天数** 的**较小者**：

- `base` = 仅当 `expiry.enabled && expiry.date` 时取 `expiry.date`。
- `opened` = 仅当 `opened.openedAt && opened.shelfDays > 0` 时取 `addDays(openedAt, shelfDays)`。
- 两者都有 → 取较早者（字符串比较 YYYY-MM-DD）；来源标注为 `base` 或 `opened`（`opened` 时文案追加「按开封后保质期」）。

### 7.2 剩余天数文案（`formatRemaining`）

| 天数 | 文案 |
|---|---|
| `days < 0` | `已过期 N 天` |
| `days === 0` | `今天到期` |
| `days > 0` | `剩 N 天` |

### 7.3 过期状态判定（`getExpiryStatus`）

- 无有效到期日 → `none`。
- `days < 0` → `expired`。
- `days <= alertDays` → `soon`。
- 否则 → `none`。

### 7.4 低库存与用完

- **低库存**：`quantity` 为数字 且 `lowStockThreshold` 非空 且 `quantity <= lowStockThreshold`。
- **用完**：`quantity === 0`。

### 7.5 搜索匹配

- 多关键词（空格拆分）**AND** 匹配；每个关键词支持**子串**或**子序列**匹配。
- 搜索字段：名称、房间、位置、标签、分类名、成员名、单位、备注、购买渠道、借给谁、到期日、开封日期。

### 7.6 软删除与归档模型

- **删除（软删除）**：物品移入 `fdl_trash`（照片保留），直到「彻底删除」或「清空回收站」才删除照片文件。
- **归档（独立于回收站）**：`archived` 物品仍存于 `fdl_items`，但从首页 / 过期 / 清单 / 统计中排除，可在首页「已归档」筛选查看。
- 删除空间时，该空间下物品一并移入回收站。

---

## 8. 数据导入与导出

### 8.1 CSV 导出

- 19 列：名称、空间、分类、房间、具体位置、归属人、数量、单位、到期日、开封日期、开封后保质天数、购买日期、购买渠道、价格、低库存阈值、借出中、标签、备注、归档。
- 格式：UTF-8 BOM 开头、`\r\n` 行分隔、含 `"` `,` 换行符的单元格自动转义。
- 文件名：`小寻物仓_备份_YYYYMMDD.csv`。
- 导出方式：「发送到微信保存」（`wx.shareFileMessage`）或「复制到剪贴板」。

### 8.2 JSON 备份 / 导入

- 导出结构：`{ version: 1, exportedAt, items, members, categories, rooms, spaces, settings }`，复制到剪贴板。
- 导入：从剪贴板解析，确认后**覆盖式**导入（**照片不导入**）。

---

## 9. 数据迁移与版本

`store.seed()` 在 App 启动时执行，依据 `settings.seedVersion`（当前为 4）做版本门控迁移：

| 版本 | 迁移内容 |
|---|---|
| v2 | 分类为空则写入默认分类；否则补全缺失的默认分类（不覆盖用户删除） |
| v3 | 老用户（已有物品）置 `onboarded = true` |
| v4 | 为每件物品补 `spaceId`（默认 `sp_home`）与 `archived`（布尔化）；房间由全局数组迁移为按空间分桶（「办公室」由房间升级为空间，迁移时剔除） |

---

## 10. 已知限制与未来展望

| 项 | 说明 |
|---|---|
| 语音录入（已移除） | 原 v4 需求 #10（语音录入）因微信「同声传译」插件在当前账号下未授权（编译报 `80082 permission deny`），且纯本地小程序在无该插件、无后端的前提下无替代语音转文字方案，故**已移除**。未来若微信开放新的语音能力或账号可授权插件，可回补（核心解析逻辑已就绪）。 |
| 无云同步 / 多端 | 纯本地存储，数据不跨设备同步；卸载或清缓存会丢失（可通过 JSON/CSV 导出备份）。 |
| 通知 | 仅在「打开 App 时」进行到期提醒，无系统级推送订阅。 |

---

## 11. 验收要点

核心流程回归清单：

1. 首次启动展示新手引导，点击开始后进入首页。
2. 添加物品（模板 / 照片 / 分类漏斗 / 保质期 / 开封后保质 / 库存借出），保存成功。
3. 物品详情四大操作：移动位置、借出/归还、用完、丢弃。
4. 丢弃后进回收站，可恢复 / 彻底删除（含照片）/ 清空。
5. 过期页展示已过期与临期，一键归档已过期物品；首页「已归档」可查看归档物品。
6. 多空间：新增、切换、删除（至少保留一个，删除时空下物品入回收站）。
7. 搜索：关键词命中名称/位置/标签/分类，搜索历史记录与清除。
8. 购物清单：低于阈值自动成单，缺货/补 N 件徽章，用完弱化（灰度 + opacity 0.55）。
9. 分类删除二次确认：空分类一次确认；有物品分类确认后「移动到其他分类 / 变为未分类」。
10. 数据：CSV/JSON 导出，JSON 导入（覆盖式、照片不导入）；清空示例数据。
11. 主题色切换即时生效；标题栏、欢迎语、导出文件名均为「小寻物仓」。
